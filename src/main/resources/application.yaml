server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  banner:
    location: banner.txt
  datasource:
    url: "jdbc:mysql://localhost:3306/identity-service"
    username: root
    password: my-root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true